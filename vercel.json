{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd aira && npm install",
  "buildCommand": "cd aira && npm run build",
  "outputDirectory": "aira/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
